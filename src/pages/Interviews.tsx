import SectionHeader from "@/components/SectionHeader";

const Interviews = () => {
  return (
    <SectionHeader
      title="Interviews"
      subtitle="Subtitle"
    />
  )

};

export default Interviews;